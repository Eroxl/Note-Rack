paths:
  /account/get-page-tree:
    get:
      description: Get a users page tree
      tags:
        - Account
        - Pages
      responses:
        '200': 
          description: Returns the page tree
        '400':
          description: The user isn't logged in